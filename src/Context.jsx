import React from "react";

const usercontext = React.createContext('JACK -DEFAULT');
const contProvider = usercontext.Provider;



// const contConsumer = usercontext.Consumer;

export default usercontext;